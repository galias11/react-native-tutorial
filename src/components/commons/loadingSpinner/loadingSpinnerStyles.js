// @Vendor
const {
    StyleSheet
} = require('react-native');

// @BaseStyles
const {
    padding
} = require('../../_base');

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: padding.md
    }
});

module.exports = styles;